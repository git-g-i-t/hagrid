# 迁移旧日志到 train 子目录

为了使现有的实验记录符合新的日志分流结构，我将执行以下操作：

## 1. 目标
遍历 `work_dir` 下的所有实验文件夹，将 `logs/` 根目录下的所有文件移动到 `logs/train/` 子目录中。

## 2. 脚本逻辑
我将创建一个临时脚本 `migrate_logs.py`，执行以下逻辑：
1. 遍历 `work_dir` 下的所有子目录（即实验名）。
2. 检查是否存在 `logs` 目录。
3. 检查 `logs` 目录下是否有文件（排除已有的 `train` 或 `test` 文件夹）。
4. 如果有文件：
   - 创建 `logs/train` 目录。
   - 将这些文件移动进去。
   - 打印迁移日志。

## 3. 预期结果
- 转换前：`work_dir/Exp1/logs/events.out...`
- 转换后：`work_dir/Exp1/logs/train/events.out...`

这样无需重新训练，旧实验也能兼容新的目录结构。

是否执行？